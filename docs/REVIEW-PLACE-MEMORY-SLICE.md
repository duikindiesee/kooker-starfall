# Grounded place-memory slice — 15 September 2026

Source commits: `2de3ddbb917d17db3660d36938641a457d2dfc1a` (ledger and live-perception wiring) and `54ada4dc805cb40d49e52d6cdf1e40de5752608d` (no false same-position revisit on restart). This is a component and narrow compiled-player checkpoint, not a playable-world or model-planning acceptance receipt.

| Claim | Status | Evidence | Remaining gap |
|---|---|---|---|
| Explored cells are grounded | Narrow player proof | `StarfallSurvivalAutonomy` records a three-metre cell only when the actor physically occupies supported terrain/refuge floor; round234 ordinary run33 saved three actually occupied cells after a model-admitted approach and reached route. | Broader travel/restart and sustained traversal |
| Place history is scoped and revisable | First-seen player proof; revision component-tested | `FoodState` stores world/generation/actor-bound, hash-chained first-seen/changed/revisit events; round234 run33 saved a live LOS `berry-food` first-seen event with observed type `fruiting-succulent`, while spring remained unknown. | Actual-player leave/re-enter, changed belief, postrestart recall and model-use proof |
| Old saves and new worlds remain isolated | Component-tested | Older `food.v1` payloads without lists restore with empty lists after validation; foreign world/inhabitant/generation and malformed locations are rejected; a fresh-world save leaves the old scoped save recoverable. | Unified full-world checkpoint and migration remain unimplemented |
| Bounded capacity is safe | Intermediate, not final | The maximum fixture (512 cells, 256 events) saved/reloaded as a 217,523-byte immutable snapshot. Exhaustion explicitly fails closed instead of silently dropping observations. | Timeline-preserving archival/compaction before sustained evolving-world acceptance |

The pinned Unity editor's checks-only method passed 114 deterministic tests at `evidence/local/editor-food-checks-20260915-170221/passed.txt` (SHA256 `f587d326b9b6105a85c3d6cef8775e2fbba73b278b557a0d91bb0ed2a14df94b`). After the restart-label correction, a second checks-only run passed 116 tests at `evidence/local/editor-food-checks-20260915-171344/passed.txt` (SHA256 `f60408fe78512aab2d545e01835ed1e00c8773920346af548780aea65a8e3fd0`). Perception supplies same-tick, stable-ID-sorted observations every ten autonomy ticks; persisted food time is monotonic, while `Brain.Tick` is session-local metadata because it resets on process restart. The tests reload a saved ledger, revise at a lower local brain tick and higher persisted food tick, avoid a false same-position revisit, then record a revisit only after visibility is lost and restored.

Compiled round234 (`evidence/milestones/coastal/round-234/preview-build.json`, source `5e0388f9c2920557a0e0f0675fc54dd68324b2e6`) was launched in ordinary mode with an empty dedicated scoped store. The process receipt and clean-exit evidence at `evidence/local/normal-survival/run-33/` bind the same 184-file hash `0bf249f22809c5eeb63d042e826092ec6447892ab510a6a1b291b4ba6db750fe` before/after. One actual local-model `approach berry` was admitted in 4866 ms and reached; the final immutable scoped snapshot (SHA256 `AB31B3C39155F59C75B080C7871B386521FF87687654C620DD7A91DFEE7EC544`) has three occupied cells and exactly one hash-chained first-seen `berry-food` event from LOS perception, type `fruiting-succulent`; `knowsSpring=false`. Nine later model requests timed out. This binary predates the same-position-restart fix, so it is **not** restart/revisit acceptance. The verified round211 executable remains fallback, and round234 remains an unverified temporary candidate.

This slice deliberately does not expose undiscovered authored registry positions to the model. Need/day planning, Telegram for the same inhabitant, broader world save, actual-player memory use and any Hermes-versus-bridge choice are separate dependent gates.
