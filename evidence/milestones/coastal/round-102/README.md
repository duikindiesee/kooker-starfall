# Corrected integrated candidate: acceptance still pending

Compiled source `562c5d32c630a6c9b7a20c43336b48236d8f9f2e`, zero build errors, 14 warnings. The dated coastal composition captures precede integrated scene attachment and are editor renders, not final player screenshots.

`runtime-02.json` is the first actual-player run of this executable. It completed one autonomous delivery, moved the possessed CharacterController about 1.9 metres and passed 13/14 automated checks. The failed check expected an exact display-mode round trip after a maximized start; observed transitions were MaximizedWindow -> Windowed -> FullScreenWindow, both applied with player state preserved. Maximized-window restoration is not implemented by the two-mode toggle.

`runtime-03.json` preserves the subsequent run with explicit windowed launch arguments. Its injected-input checks failed while other local player windows were active. The test devices were still subject to Input System focus-based disabling; this run cannot establish native control failure or success. Follow-up source isolates synthetic test devices under the smoke flag only, without changing normal player's focus handling.

Visual review rejected thick opaque rainfall and the loss of the composed forward galaxy band after replacing its plane. Follow-up source retains the composed plane with surrounding stars and thinner translucent rain, but this build does not prove those changes. Native keyboard presses through the Windows tool did not produce an observable options-menu response; direct user verification was requested. No native control acceptance is claimed.

Club remains excluded. The existing scene is a finite integration checkpoint with placeholder refuge geometry. Full world, swimming, boats, living sea, complete saves, live model thought, food and refuge safety remain outside this build's proven capabilities. Preserve both failed reports.
